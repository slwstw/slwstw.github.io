# 301 永久轉址到新網站
# 將所有舊網站流量導向新的 GitHub Pages

# 首頁轉址
RewriteEngine On
RewriteRule ^$ https://slwstw.github.io/ [R=301,L]

# 所有頁面都轉址到新網站對應頁面
RewriteRule ^(.*)$ https://slwstw.github.io/$1 [R=301,L]

# 如果不支援 RewriteRule，可以使用 Redirect 指令
# Redirect 301 / https://slwstw.github.io/